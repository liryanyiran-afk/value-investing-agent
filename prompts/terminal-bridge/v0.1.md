# terminal-bridge prompt — v0.1

> Placeholder. 真实 prompt 待 03 负责人定.

## 元信息
- agent: 03 terminal-bridge
- version: v0.1
- date: 2026-08-08
- author: _待_
- 跑分: _待_

## 目标
封装 Wind / Bloomberg / Choice / iFinD 终端 API, 暴露统一数据点 (价格 / 财务 / 估值 / 指数成分 / 分红 / 卖方预期).

## 待填
- 系统 prompt
- 凭证加载逻辑 (从 `~/.config/vi-agent/credentials.yaml`)
- 退避重试 + fallback 策略
- 输出 schema
- 缓存策略

## 已知约束
- 凭证**绝不入仓** (见根 `.gitignore`)
- 凭证放 `~/.config/vi-agent/credentials.yaml` (不进仓)
- 调脚本前需 env var 注入
- 时区全部统一到 HKT (GMT+8)
- 限流: 退避 + 队列

## 下一步
- 确认 Wind 凭证 (2Mind 是否给)
- 选第一版支持哪个终端 (推荐 wind 优先, choice 兜底)
- 跑通 price_history 端点, 在 0700.HK 上验证
